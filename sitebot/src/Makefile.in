CC=@CC@
RM=rm -f
MAKE=make

CFLAGS=@CFLAGS@ -W -Wall @LCRYPT@ @LCRYPTO@ -O2 -I../../

all: passchk.c ng-bw.c
	${CC} ${CFLAGS} -o passchk passchk.c
	${CC} ${CFLAGS} -o ng-bandwidth ng-bw.c

distclean: clean
	$(RM) Makefile

clean:
	$(RM) ng-bandwidth passchk
