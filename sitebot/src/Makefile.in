CC=@CC@
RM=rm -f
MAKE=make

CFLAGS=@CFLAGS@ -W -Wall @GLVERSION@ -I../../

all: passchk ng-bw

passchk: passchk.c
	$(CC) $(LDFLAGS) $(CFLAGS) -o passchk passchk.c @LCRYPT@ @LCRYPTO@

ng-bw: ng-bw.c
	$(CC) $(LDFLAGS) $(CFLAGS) -o ng-bw ng-bw.c @LCRYPT@ @LCRYPTO@

distclean: clean

clean:
	$(RM) ng-bw passchk
