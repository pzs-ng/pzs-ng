# $Id: Makefile.in,v 1.11 2000/06/19 17:04:34 zap Exp $
prefix=@prefix@

#CFLAGS=-O2 -Wall
CFLAGS=-Wall -g
CC=@CC@
LIBS=@LIBS@
ZSLIBS=@ZSLIBS@
CFGLIBS=@CFGLIBS@
ZSTYPE=@ZSTYPE@

all: zs
#Configurator

install: zs
	@( cd zipscript/src ; $(MAKE) install )

distclean: clean
	@( rm -f *~ Makefile config.cache config.log config.status config.h )

clean:
	@( cd zipscript/src ; $(MAKE) clean )

zs:
	@( cd zipscript/src ; $(MAKE) all )

bot:
	@( cd sitebot/src ; ${MAKE} all )
#Configurator:
#	@( if test x$(CFGLIBS) != x ; then cd configurator ; $(MAKE) all ; fi )
