You should no longer need to compile sitewho from here - everything is done
from the main dir. 'make install' will copy the config, header and footer files
along with the compiled bin into your glftpd setup.
If the header/footer/config files are already present, it will NOT overwrite
your copies.

